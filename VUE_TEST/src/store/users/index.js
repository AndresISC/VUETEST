import usersAPI from '../../api/users'
import * as types from '../types'

const namespaced = true

const state = {
    
}

const mutations = {
    
}

const actions = {
    
}

const getters = {
}

export default {
    namespaced,
    state,
    mutations,
    actions,
    getters
}